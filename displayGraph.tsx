// import React from 'react';
// import { ReactElement, useState } from 'react';
// import BarChart from '../graph/graph';

// export interface DisplayGraphInterface {
//   startDate: string;
//   endDate: string;
// }
// const actions = (startDate: string, endDate: string) => {getActions};
// const yValueFn = (action: GraphType) => action.numScans;
// const xValueFn = (action: GraphType) => action.action;
// const [openGraph, setGraph] = useState(false);

// const displayGraph = (values, yValueFn, xLabelFn): ReactElement | undefined => {
//   setGraph(true);
//   if (openGraph === true) {
//     return <BarChart values={actions} yValueFn={yValueFn} xLabelFn={xValueFn} />;
//   }
//   return;
// };
// export default displayGraph;
